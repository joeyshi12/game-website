export class Camera {
    constructor(width, height) {
        this.x = player.x;
        this.y = player.y;
        this.width = width;
        this.height = height;
    }
}

let camera = new Camera(200, 200);