class Camera {
    constructor(center_x, center_y) {
        this.center_x = center_x;
        this.center_y = center_y;
        this.x = center_x;
        this.y = center_y;
    }

    update() {
        
    }
}